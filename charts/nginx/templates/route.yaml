kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ .Chart.name }}
  labels:
    {{- include "nginx.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ .Chart.name }}
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
